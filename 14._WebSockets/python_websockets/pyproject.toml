[tool.poetry]
name = "python-websockets"
version = "0.1.0"
description = ""
authors = ["fundish <funderchristian@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
websockets = "^12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
